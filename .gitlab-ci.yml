# Generated from .gitlab-ci.jsonnet 
#     DO NOT EDIT THIS FILE BY HAND -- YOUR CHANGES WILL BE OVERWRITTEN 
---
container-base-build:
  before_script:
  - docker login -u $DOCKER_USER -p $DOCKER_PASS quay.io
  image: docker:git
  only:
  - schedules
  - tags
  script:
  - 'docker build --build-arg sshkey=$OPERATORCLENT_RSA_B64 --no-cache -f base.Dockerfile -t quay.io/coreos/alm-ci:base . '
  - docker push quay.io/coreos/alm-ci:base
  stage: docker_base
  tags:
  - kubernetes
  variables:
    DOCKER_DRIVER: overlay2
    DOCKER_HOST: tcp://docker-host.gitlab-runner.svc.cluster.local:2375
container-build:
  before_script:
  - docker login -u $DOCKER_USER -p $DOCKER_PASS quay.io
  - mkdir -p $PWD/bin
  image: docker:git
  script:
  - 'docker build  --no-cache -f alm-ci.Dockerfile -t quay.io/coreos/alm-ci:${CI_COMMIT_REF_SLUG} . '
  - docker push quay.io/coreos/alm-ci:${CI_COMMIT_REF_SLUG}
  - 'docker build  --no-cache -f catalog-ci.Dockerfile -t quay.io/coreos/catalog-ci:${CI_COMMIT_REF_SLUG} . '
  - docker push quay.io/coreos/catalog-ci:${CI_COMMIT_REF_SLUG}
  - docker create quay.io/coreos/alm-ci:${CI_COMMIT_REF_SLUG} | xargs -I{} docker cp {}:/bin/alm bin/alm
  - docker create quay.io/coreos/catalog-ci:${CI_COMMIT_REF_SLUG} | xargs -I{} docker cp {}:/bin/catalog bin/catalog
  - 'docker build  --no-cache -f alm-pre.Dockerfile -t quay.io/coreos/alm-ci:${CI_COMMIT_REF_SLUG}-pre . '
  - docker push quay.io/coreos/alm-ci:${CI_COMMIT_REF_SLUG}-pre
  - 'docker build  --no-cache -f catalog-pre.Dockerfile -t quay.io/coreos/catalog-ci:${CI_COMMIT_REF_SLUG}-pre . '
  - docker push quay.io/coreos/catalog-ci:${CI_COMMIT_REF_SLUG}-pre
  - 'docker build  --no-cache -f e2e-run.Dockerfile -t quay.io/coreos/alm-e2e:${CI_COMMIT_REF_SLUG}-${SHA8} . '
  - docker push quay.io/coreos/alm-e2e:${CI_COMMIT_REF_SLUG}-${SHA8}
  stage: docker_build
  tags:
  - kubernetes
  variables:
    DOCKER_DRIVER: overlay2
    DOCKER_HOST: tcp://docker-host.gitlab-runner.svc.cluster.local:2375
container-release:
  before_script:
  - docker login -u $DOCKER_USER -p $DOCKER_PASS quay.io
  - mkdir -p $PWD/bin
  image: docker:git
  only:
  - master
  - tags
  script:
  - docker pull quay.io/coreos/alm-ci:${CI_COMMIT_REF_SLUG}-pre
  - docker tag quay.io/coreos/alm-ci:${CI_COMMIT_REF_SLUG}-pre quay.io/coreos/alm:${CI_COMMIT_REF_SLUG}-${SHA8}
  - docker push quay.io/coreos/alm:${CI_COMMIT_REF_SLUG}-${SHA8}
  - docker pull quay.io/coreos/catalog-ci:${CI_COMMIT_REF_SLUG}-pre
  - docker tag quay.io/coreos/catalog-ci:${CI_COMMIT_REF_SLUG}-pre quay.io/coreos/catalog:${CI_COMMIT_REF_SLUG}-${SHA8}
  - docker push quay.io/coreos/catalog:${CI_COMMIT_REF_SLUG}-${SHA8}
  - docker pull quay.io/coreos/alm-e2e:${CI_COMMIT_REF_SLUG}-${SHA8}
  - docker tag quay.io/coreos/alm-e2e:${CI_COMMIT_REF_SLUG}-${SHA8} quay.io/coreos/alm-e2e:latest
  - docker push quay.io/coreos/alm-e2e:latest
  stage: docker_release
  tags:
  - kubernetes
  variables:
    DOCKER_DRIVER: overlay2
    DOCKER_HOST: tcp://docker-host.gitlab-runner.svc.cluster.local:2375
deploy-preview:
  before_script:
  - 'echo "version: 1.0.0-${CI_COMMIT_REF_SLUG}-pre" >> deploy/chart/kube-1.8/Chart.yaml'
  - 'echo "{\"alm.image.ref\": \"quay.io/coreos/alm-ci:${CI_COMMIT_REF_SLUG}-pre\", \"catalog.image.ref\": \"quay.io/coreos/catalog-ci:${CI_COMMIT_REF_SLUG}-pre\", \"catalog_namespace\": \"tectonic-system\",
    \"namespace\": \"ci-alm-${CI_COMMIT_REF_SLUG}\", \"watchedNamespaces\": \"ci-alm-${CI_COMMIT_REF_SLUG}\"}" > params.json'
  - cat params.json
  environment:
    name: review/ci-alm-${CI_COMMIT_REF_SLUG}
    on_stop: stop-preview
    url: https://alm-${CI_COMMIT_REF_SLUG}.k8s.devtable.com
  except:
  - master
  - tags
  image: quay.io/coreos/alm-ci-build:latest
  only:
  - branches
  script:
  - echo $CD_KUBECONFIG | base64 -d > kubeconfig
  - export KUBECONFIG=./kubeconfig
  - kubectl create ns ci-alm-${CI_COMMIT_REF_SLUG} || true
  - kubectl create secret docker-registry coreos-pull-secret --docker-server quay.io --docker-username $DOCKER_USER --docker-password $DOCKER_PASS --docker-email ignored@example.com --namespace=ci-alm-${CI_COMMIT_REF_SLUG}
    || true
  - charttmpdir=`mktemp -d 2>/dev/null || mktemp -d -t 'charttmpdir'`;mkdir -p ${charttmpdir};pushd deploy/chart/kube-1.8/templates;filenames=$(ls *.yaml);popd;for f in ${filenames};do helm template --set
    namespace=ci-alm-${CI_COMMIT_REF_SLUG} deploy/chart/kube-1.8 -x templates/${f} --set alm.image.ref=quay.io/coreos/alm-ci:${CI_COMMIT_REF_SLUG}-pre --set catalog.image.ref=quay.io/coreos/catalog-ci:${CI_COMMIT_REF_SLUG}-pre
    --set catalog_namespace=tectonic-system --set namespace=ci-alm-${CI_COMMIT_REF_SLUG} --set watchedNamespaces=ci-alm-${CI_COMMIT_REF_SLUG} > ${charttmpdir}/${f};done;kubectl apply -f ${charttmpdir}
  - kubectl rollout status -w deployment/alm-operator --namespace=ci-alm-${CI_COMMIT_REF_SLUG}
  - kubectl rollout status -w deployment/catalog-operator --namespace=ci-alm-${CI_COMMIT_REF_SLUG}
  stage: deploy_preview
  tags:
  - kubernetes
  variables:
    ALM_DOMAIN: alm-${CI_COMMIT_REF_SLUG}.k8s.devtable.com
    K8S_NAMESPACE: ci-alm-${CI_COMMIT_REF_SLUG}
  when: manual
deploy-staging:
  before_script:
  - 'echo "version: 1.0.0-${CI_COMMIT_REF_SLUG}-pre" >> deploy/chart/kube-1.8/Chart.yaml'
  - 'echo "{\"alm.image.ref\": \"quay.io/coreos/alm:${CI_COMMIT_REF_SLUG}-${SHA8}\", \"catalog.image.ref\": \"quay.io/coreos/catalog:${CI_COMMIT_REF_SLUG}-${SHA8}\", \"catalog_namespace\": \"tectonic-system\",
    \"namespace\": \"ci-alm-staging\", \"watchedNamespaces\": \"ci-alm-staging\"}" > params.json'
  - cat params.json
  environment:
    name: staging
    url: https://alm-staging.k8s.devtable.com
  image: quay.io/coreos/alm-ci-build:latest
  only:
  - master
  script:
  - echo $CD_KUBECONFIG | base64 -d > kubeconfig
  - export KUBECONFIG=./kubeconfig
  - kubectl create ns ci-alm-staging || true
  - kubectl create secret docker-registry coreos-pull-secret --docker-server quay.io --docker-username $DOCKER_USER --docker-password $DOCKER_PASS --docker-email ignored@example.com --namespace=ci-alm-staging
    || true
  - charttmpdir=`mktemp -d 2>/dev/null || mktemp -d -t 'charttmpdir'`;mkdir -p ${charttmpdir};pushd deploy/chart/kube-1.8/templates;filenames=$(ls *.yaml);popd;for f in ${filenames};do helm template --set
    namespace=ci-alm-staging deploy/chart/kube-1.8 -x templates/${f} --set alm.image.ref=quay.io/coreos/alm:${CI_COMMIT_REF_SLUG}-${SHA8} --set catalog.image.ref=quay.io/coreos/catalog:${CI_COMMIT_REF_SLUG}-${SHA8}
    --set catalog_namespace=tectonic-system --set namespace=ci-alm-staging --set watchedNamespaces=ci-alm-staging > ${charttmpdir}/${f};done;kubectl apply -f ${charttmpdir}
  - kubectl rollout status -w deployment/alm-operator --namespace=ci-alm-staging
  - kubectl rollout status -w deployment/catalog-operator --namespace=ci-alm-staging
  stage: deploy_staging
  tags:
  - kubernetes
  variables:
    ALM_DOMAIN: alm-staging.k8s.devtable.com
    K8S_NAMESPACE: ci-alm-staging
e2e-setup:
  before_script:
  - 'echo "version: 1.0.0-${CI_COMMIT_REF_SLUG}-pre" >> deploy/chart/kube-1.8/Chart.yaml'
  - 'echo "{\"alm.image.ref\": \"quay.io/coreos/alm-ci:${CI_COMMIT_REF_SLUG}-pre\", \"catalog.image.ref\": \"quay.io/coreos/catalog-ci:${CI_COMMIT_REF_SLUG}-pre\", \"catalog_namespace\": \"e2e-${CI_COMMIT_REF_SLUG}\",
    \"namespace\": \"e2e-${CI_COMMIT_REF_SLUG}\", \"watchedNamespaces\": \"e2e-${CI_COMMIT_REF_SLUG}\"}" > params.json'
  - cat params.json
  environment:
    name: review/e2e-${CI_COMMIT_REF_SLUG}
    url: https://alm-${CI_COMMIT_REF_SLUG}.k8s.devtable.com
  image: quay.io/coreos/alm-ci-build:latest
  script:
  - echo $CD_KUBECONFIG | base64 -d > kubeconfig
  - export KUBECONFIG=./kubeconfig
  - kubectl create ns e2e-${CI_COMMIT_REF_SLUG} || true
  - kubectl create secret docker-registry coreos-pull-secret --docker-server quay.io --docker-username $DOCKER_USER --docker-password $DOCKER_PASS --docker-email ignored@example.com --namespace=e2e-${CI_COMMIT_REF_SLUG}
    || true
  - charttmpdir=`mktemp -d 2>/dev/null || mktemp -d -t 'charttmpdir'`;mkdir -p ${charttmpdir};pushd deploy/chart/kube-1.8/templates;filenames=$(ls *.yaml);popd;for f in ${filenames};do helm template --set
    namespace=e2e-${CI_COMMIT_REF_SLUG} deploy/chart/kube-1.8 -x templates/${f} --set alm.image.ref=quay.io/coreos/alm-ci:${CI_COMMIT_REF_SLUG}-pre --set catalog.image.ref=quay.io/coreos/catalog-ci:${CI_COMMIT_REF_SLUG}-pre
    --set catalog_namespace=e2e-${CI_COMMIT_REF_SLUG} --set namespace=e2e-${CI_COMMIT_REF_SLUG} --set watchedNamespaces=e2e-${CI_COMMIT_REF_SLUG} > ${charttmpdir}/${f};done;kubectl apply -f ${charttmpdir}
  - kubectl rollout status -w deployment/alm-operator --namespace=e2e-${CI_COMMIT_REF_SLUG}
  - kubectl rollout status -w deployment/catalog-operator --namespace=e2e-${CI_COMMIT_REF_SLUG}
  stage: test_setup
  tags:
  - kubernetes
  variables:
    ALM_DOMAIN: alm-${CI_COMMIT_REF_SLUG}.k8s.devtable.com
    K8S_NAMESPACE: e2e-${CI_COMMIT_REF_SLUG}
e2e-teardown:
  before_script: []
  environment:
    action: stop
    name: review/e2e-${CI_COMMIT_REF_SLUG}
    url: https://alm-${CI_COMMIT_REF_SLUG}.k8s.devtable.com
  image: quay.io/coreos/alm-ci-build:latest
  script:
  - echo $CD_KUBECONFIG | base64 -d > kubeconfig
  - export KUBECONFIG=./kubeconfig
  - kubectl delete ns --ignore-not-found=true e2e-${CI_COMMIT_REF_SLUG}
  - kubectl get pods -o wide -n e2e-${CI_COMMIT_REF_SLUG}
  stage: test_teardown
  tags:
  - kubernetes
  variables:
    ALM_DOMAIN: alm-${CI_COMMIT_REF_SLUG}.k8s.devtable.com
    GIT_STRATEGY: none
    K8S_NAMESPACE: e2e-${CI_COMMIT_REF_SLUG}
e2e_tests:
  image: quay.io/coreos/alm-ci-build:latest
  script:
  - echo $CD_KUBECONFIG | base64 -d > kubeconfig
  - export KUBECONFIG=./kubeconfig
  - kubectl create secret docker-registry coreos-pull-secret --docker-server quay.io --docker-username $DOCKER_USER --docker-password $DOCKER_PASS --docker-email ignored@example.com --namespace=e2e-${CI_COMMIT_REF_SLUG}
    || true
  - 'kubectl -n e2e-${CI_COMMIT_REF_SLUG} patch serviceaccount default -p ''{"imagePullSecrets": [{"name": "coreos-pull-secret"}]}'''
  - kubectl -n e2e-${CI_COMMIT_REF_SLUG} create rolebinding e2e-admin-rb --clusterrole=cluster-admin --serviceaccount=e2e-${CI_COMMIT_REF_SLUG}:default --namespace=e2e-${CI_COMMIT_REF_SLUG} || true
  - charttmpdir=`mktemp -d 2>/dev/null || mktemp -d -t 'charttmpdir'`;mkdir -p ${charttmpdir};pushd e2e/chart/templates;filenames=$(ls *.yaml);popd;for f in ${filenames};do helm template --set namespace=e2e-${CI_COMMIT_REF_SLUG}
    e2e/chart -x templates/${f} --set e2e.image.ref=quay.io/coreos/alm-e2e:${CI_COMMIT_REF_SLUG}-${SHA8} --set job_name=e2e-${CI_COMMIT_REF_SLUG}-${SHA8} --set namespace=e2e-${CI_COMMIT_REF_SLUG} > ${charttmpdir}/${f};done;kubectl
    apply -f ${charttmpdir}
  - until kubectl -n e2e-${CI_COMMIT_REF_SLUG} logs job/e2e-${CI_COMMIT_REF_SLUG}-${SHA8} | grep -v 'ContainerCreating'; do echo 'waiting for job to run' && sleep 1; done
  - kubectl -n e2e-${CI_COMMIT_REF_SLUG} logs job/e2e-${CI_COMMIT_REF_SLUG}-${SHA8} -f
  - if kubectl -n e2e-${CI_COMMIT_REF_SLUG} logs job/e2e-${CI_COMMIT_REF_SLUG}-${SHA8} 2>&1 | grep -q '^not'; then kubectl -n e2e-${CI_COMMIT_REF_SLUG} logs -l app=alm; exit 1; else exit 0; fi
  stage: tests
  tags:
  - kubernetes
  variables:
    K8S_NAMESPACE: e2e-${CI_COMMIT_REF_SLUG}
    NAMESPACE: e2e-${CI_COMMIT_REF_SLUG}
stages:
- docker_base
- docker_build
- deploy_preview
- test_setup
- tests
- test_teardown
- integration
- docker_release
- deploy_staging
- teardown
stop-preview:
  before_script: []
  environment:
    action: stop
    name: review/ci-alm-${CI_COMMIT_REF_SLUG}
    url: https://alm-${CI_COMMIT_REF_SLUG}.k8s.devtable.com
  except:
  - master
  - tags
  image: quay.io/coreos/alm-ci-build:latest
  only:
  - branches
  script:
  - echo $CD_KUBECONFIG | base64 -d > kubeconfig
  - export KUBECONFIG=./kubeconfig
  - kubectl delete ns --ignore-not-found=true ci-alm-${CI_COMMIT_REF_SLUG}
  - kubectl get pods -o wide -n ci-alm-${CI_COMMIT_REF_SLUG}
  stage: deploy_preview
  tags:
  - kubernetes
  variables:
    ALM_DOMAIN: alm-${CI_COMMIT_REF_SLUG}.k8s.devtable.com
    GIT_STRATEGY: none
    K8S_NAMESPACE: ci-alm-${CI_COMMIT_REF_SLUG}
  when: manual
unit-tests:
  before_script:
  - mkdir -p $GOPATH/src/github.com/coreos-inc/alm
  - cp -a $CI_PROJECT_DIR/* $GOPATH/src/github.com/coreos-inc/alm
  - cd $GOPATH/src/github.com/coreos-inc/alm
  coverage: /\d\d\.\d.$/
  image: quay.io/coreos/alm-ci:${CI_COMMIT_REF_SLUG}
  script:
  - make update-catalog && git diff --exit-code
  - make vendor
  - make test-cover
  stage: tests
  tags:
  - kubernetes
variables:
  FAILFASTCI_NAMESPACE: quay
