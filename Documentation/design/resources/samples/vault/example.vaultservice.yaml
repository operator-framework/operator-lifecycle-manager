#! validate-crd: samples/vault/vaultservice.crd.yaml
apiVersion: security.coreos.com/v1alpha1
kind: VaultService
metadata:
  namespace: default
  name: my-vault-service
  labels:
    operated-by: vaultoperator.0.0.2
    alm-owner-vault: vault.apptypev1s.app.coreos.com.v1alpha1 
spec:
  version: "0.8.0-0"
  nodes: 1
  labels:
     vault-service: my-vault-service
outputs:
  vault-service-name: my-vault-cluster
