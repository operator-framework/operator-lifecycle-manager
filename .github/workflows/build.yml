name: build
on:
  merge_group:
  pull_request:
  workflow_dispatch:
jobs:
  image:
    runs-on: ubuntu-latest
    steps:
    - name: Check out the repo
      uses: actions/checkout@v3
    - name: Build binaries
      run: make build
    - name: Build the container image
      run: make container
